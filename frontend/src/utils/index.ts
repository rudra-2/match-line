/**
 * Utility Functions - Barrel export
 *
 * Usage:
 * import { formatDate, truncate, getScoreColor } from '@/utils'
 */

export {
  getScoreColor,
  getScoreBgColor,
  formatDate,
  truncate,
  getInitials,
} from './formatting'
